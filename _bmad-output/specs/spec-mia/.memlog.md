---
topic: MIA self-hosted AI tutoring platform backend MVP
updated: 2026-09-13T18:16
---

- (direction) User chose to distill prd-mia-2026-08-29 (prd.md + addendum.md) into the spec; slug inherited: mia
- (decision) Express distill: input is a final, pre-sorted PRD; trust authored separation, no elicitation
- (decision) prd.md and addendum.md adopted as companions, not absorbed: FR-1..84, NFR-1..21, AS-1..32 and technical depth are load-bearing line-item detail downstream must read; duplication would fork the source of truth
- (decision) ARCHITECTURE-SPINE.md adopted as companion: AD-1..AD-14, conventions, stack, structural seed bind implementation; spine already reconciled against PRD and repo docs
- (decision) reconcile-* and review-* files in PRD and architecture folders are process metadata, excluded from companions and sources
- (decision) sources: [] — nothing fully absorbed; all three inputs stay readable companions
- (decision) FR-81 (RFC3339 UTC instants) routed to Constraints; FR-82 (no notifications) routed to Non-goals; both are not capabilities
- (capability) CAP-1 identity and account data per FR-1..5 (validated username/email/profile fields, normalization rules)
- (capability) CAP-2 invitation-only staff registration and direct role grants per FR-6..14 incl. bootstrap-admin
- (capability) CAP-3 student provisioning and course membership per FR-15..17
- (capability) CAP-4 password policy and recovery per FR-18..21
- (capability) CAP-5 stateless browser sessions and staged login per FR-22..25
- (capability) CAP-6 optional MFA (TOTP/SMS), recovery codes, resets per FR-26..32
- (capability) CAP-7 profiles, avatars, mobile verification per FR-33..36
- (capability) CAP-8 course lifecycle and membership per FR-37..44
- (capability) CAP-9 learning material upload, processing, briefs, approval per FR-45..53
- (capability) CAP-10 tutoring sessions with grounded, budgeted AI tutor per FR-54..64
- (capability) CAP-11 mentoring as last resource per FR-65..71
- (capability) CAP-12 text-to-speech for completed tutor responses per FR-72..73
- (capability) CAP-13 student-only bans per FR-74
- (capability) CAP-14 account deletion and data lifecycle per FR-75..78
- (capability) CAP-15 audit log per FR-79..80
- (capability) CAP-16 background jobs and oversight per FR-83..84
- (constraint) FR/NFR text in adopted PRD is the binding launch-blocking contract; spec capabilities index it, never override it
- (constraint) AD-1..AD-14 plus spine conventions/stack bind implementation structure
- (constraint) security baseline NFR-1..8: existence-hiding, bounded inputs, redaction, content-free audit — population may include minors
- (constraint) model output never authorizes actions; fixed 32k/2048 token budgets; bounded retrieval
- (constraint) single binary, single process/node, SQLite+data-dir one backup set, provider outages degrade not down
- (constraint) rate limits, password/session/MFA policies fixed by MIA, not operator-configurable
- (constraint) instants RFC3339 UTC Z (FR-81); tests never call paid providers (NFR-21)
- (decision) Non-goals lifted from PRD section 3 confirmed exclusions plus frontend-out-of-scope (separate repo)
- (decision) Success signal: PRD section 12 product-owner decision — all AS-1..32 pass, no open critical security findings; outcome metrics deliberately deferred
- (question) Open questions carried from PRD section 13 (items not resolved by the spine): brief field limits, CSP, audit allowlist growth, brief regeneration path, interrupted-retry eligibility, long-session supervisor visibility, per-IP limits vs school NAT, non-pending invitation token response shape, 12h anchor for MFA logins, corrected-vs-original summary in next context, mentor minimal-identity field set
- (decision) Per-route API contracts stay deferred by design (PRD 13.6, spine Deferred) — recorded as deferral, not an open question
- (event) Self-validate pass 1 (coherence): PASS — Spec Law 1-6,8 satisfied; 16 capabilities, 9 constraints, 15 non-goals, testable success signal
- (event) Self-validate pass 2 (preservation): PASS — all load-bearing content preserved via adopted companions (prd.md, addendum.md, ARCHITECTURE-SPINE.md); wrapper-only drops: reconcile-*/review-* process files
- (note) MD041 (blockquote before H1) accepted: mandated by the spec template; no lines exceed 120 chars
- (direction) Story Breakdown: 13-story slicing accepted as proposed; spec_checkpoint=false everywhere; done_checkpoint=true after every story; no invoke_dev_with notes
- (decision) Story 1 kernel foundation: cmd wiring, config, lock, sqlite+migrations, identity, httpserver kernel, logging, serve skeleton
- (decision) Story 2 first auth slice: user+auth packages, bootstrap-admin, password login, staged stateless sessions, forced password change, audit kernel (CAP-1, CAP-5, parts 2/4/15)
- (decision) Story 3 staff password recovery: SMTP adapter, reset challenges, recovery emails (rest of CAP-4)
- (decision) Story 4 MFA: TOTP/SMS, challenges, recovery codes, management proofs, reset-admin-mfa, ClickSend adapter (CAP-6)
- (decision) Story 5 invitations and role grants: invitation lifecycle, acceptance, revocation, direct grants (rest of CAP-2)
- (decision) Story 6 profiles and avatars: staff self-edit, avatar/logo pipeline, mobile verification (CAP-7)
- (decision) Story 7 courses: creation, supervisor assignment, activation gating, deactivation, deletion, lifecycle registry (CAP-8)
- (decision) Story 8 student provisioning, membership, bans: provisioning, add/remove, temp passwords with cookie invalidation, ban/unban (CAP-3, CAP-13, FR-19)
- (decision) Story 9 material and processing: material+jobs packages, Mistral OCR, local parsers, briefs, approval, downloads, jobs oversight API (CAP-9, CAP-16)
- (decision) Story 10 tutoring sessions: streaming+SSE, retrieval, budgets, completion summaries, supervisor review (CAP-10)
- (decision) Story 11 mentoring: requests, triage, assignment, scheduling, removal/reassignment (CAP-11)
- (decision) Story 12 text-to-speech: ElevenLabs adapter, cache lifecycle, retention (CAP-12)
- (decision) Story 13 account deletion and audit API: deletion cascades via lifecycle registry, admin audit-log API (CAP-14, CAP-15)
- (event) stories.yaml schema check: PASS — 13 entries, string ids 1..13 unique and prefix-free, required fields present, no status field, done_checkpoint=true on all
- (decision) Story Breakdown update: accepted Epic 2 stories 2-1 through 2-12 in epics.md order; spec_checkpoint=false and done_checkpoint=true for every Epic 2 story.
- (decision) Epic 2 stories use new IDs 14 through 25: retained Epic 1 id 2 makes composite IDs 2-1 through 2-12 invalid prefix collisions; sprint keys map by order and title.
- (event) stories.yaml schema check: PASS — 25 entries, IDs 1..25 are strings, unique, and prefix-free; 12 ordered Epic 2 entries match epics.md titles with spec_checkpoint=false and done_checkpoint=true.
