---
scope: MIA backend MVP — whole system
purpose: Build substrate: consistency contract for independently built vertical slices
altitude: initiative→features (system-wide spine)
updated: 2026-08-29T17:20
---

- (constraint) ADOPTED: Go >=1.27, module github.com/thorstenkramm/mia; one executable mia with serve/bootstrap-admin/reset-admin-mfa; shared impl of config/persistence/policy/audit (docs/architecture.md)
- (constraint) ADOPTED: modernc.org/sqlite, fixed data_dir/mia.sqlite3, WAL, synchronous=FULL, FK on, 5s busy timeout, pool 4 open/4 idle, embedded golang-migrate v4 forward-only, dirty/newer schema refuses start
- (constraint) ADOPTED: Echo 5.3.1; stateless signed+encrypted CookieStore sessions; JSON:API under /api/v1; CSRF echo middleware + Fetch Metadata + token; same-origin only; per-request DB state reload; security-generation invalidation
- (constraint) ADOPTED: exclusive OS lock on mia.lock before SQLite open in every DB-using command; offline commands validate only required config subset, never contact providers
- (constraint) ADOPTED: provider adapters allowlist fields, fixed deadlines per provider, no request-path auto-retry; jobs table with 2min lease/30s renewal/3 attempts/guarded zero-row commits; adapter+route contracts written per vertical slice, auth slice first
- (constraint) ADOPTED: data-dir layout with ID-derived paths, atomic-rename publication tied to DB commit, startup integrity sequence (speech cleanup, stranded failure, missing-file fatal, orphan reconciliation); 30s graceful shutdown
- (constraint) ADOPTED: tiktoken-go o200k_base tokenizer, embedded tzdata, x/text BCP47, embedded SecLists top-100k, cases.Fold+NFC uniqueness keys (no NOCASE); Argon2id fixed params; tokens stored as SHA-256 digests
- (constraint) ADOPTED: initial packages limited to command wiring, config, locking, sqlite/migrations, identity, HTTP, first auth slice; feature packages added only with implementation, no speculative domain tree
- (decision) Paradigm: vertical feature packages over a thin shared kernel (config, sqlite, identity, http middleware, audit, providers); binds all feature work; prevents horizontal-layer drift and speculative hexagonal ceremony; user confirmed
- (decision) Data ownership: strict table ownership per feature package; cross-feature access only via owner's exported Go API; cross-feature single-transaction operations pass shared *sql.Tx, initiating operation owns tx boundary; prevents invisible table coupling and orphaned tx contracts; user confirmed
- (decision) Authorization: authorization-scoped fetch — resources reachable only through fetch functions taking actor context, identical not-found for missing and out-of-scope; handlers never fetch-then-check; prevents existence leaks across slices; user confirmed
- (decision) Work execution: three fixed systems — jobs table (OCR/material summary/session summary), in-memory tutor-response manager (SSE, 16KiB/1s persistence batching, cancel-and-fail shutdown), speech cache lifecycle; tutor responses and speech never become jobs-table jobs; user confirmed
- (decision) Commit discipline shared by all three work systems: lease/state-guarded UPDATEs; zero-row result = target gone/stale: discard results, remove newly published files, never upsert/requeue/retry; user confirmed
- (decision) Errors: one shared domain-error type with stable machine code; one registry for API error codes, audit codes, sanitized failure codes; single HTTP mapping layer to JSON:API; retryable/permanent classification only at provider-adapter boundary; wrap with %w preserving identity; user confirmed
- (decision) Structural seed: cmd/mia + internal/{config,lock,sqlite,identity,httpserver,audit,provider/*,jobs,auth,...} + embedded migrations/; everything internal; feature packages appear only with implementation; user confirmed
- (question) Deferred: GET /jobs visibility of tutor/speech work; per-route API contracts (auth slice first); provider adapter contracts at implementation; CSP with frontend; temp-file fsync strategy; brief per-field limits; error-code catalog contents grow per slice
- (change) Reconciliation fixes: AD-5 attempt-aware requeue + no-generic-retry rule, AD-11 deletion ordering, conventions rows for credentials/emailed links/data-dir perms, capability map FR-15..17 split to course/user, seed tree package-timing notes, 3 addendum tensions added to Deferred
- (decision) AD-13 added (gate C1): user ships with auth slice, owns users/user_roles/security state; single CreateAccount+GrantRoles for invited/provisioned/bootstrap shapes; cmd never issues feature SQL
- (decision) AD-14 added (gate C2): canonical import direction material/tutoring/mentoring/speech -> course -> user, never back; kernel lifecycle registry with course-scoped and account-scoped deleters registered at wiring
- (change) Gate tightenings: AD-3 single kernel Tx type, AD-5 queryable job subjects + handler registration + delete-by-subject, AD-8 route-registration gate + enumerated security-generation triggers/non-triggers, AD-10 kernel SMS send-limit gate, AD-11 kernel file-publication helper, AD-12 audit-only fingerprints; conventions rows logging/rate-limiter/request-ID idempotency/name normalization/registry naming/API query grammar/NFR-5 plaintext posture; operational envelope subsection; capability map FR-81/82 + NFR rows; stack echo-contrib v5 + tokenizer qualification; deferred list updated
- (event) spine finalized
